// enumeration of the colors used in this grid
export var colors = {
    BLACK: 'black',
    BLUE: 'blue',
    RED: 'red',
    GREEN: 'green'
   
}

// list of colors which can be cycled through

let selector = Object.keys(colors);

